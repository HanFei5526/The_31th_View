{
  "name": "kiro-prologue-server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "《卅一景》一体化服务端 — DeepSeek API 代理 + 前端静态文件",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:frontend": "cd .. && npm install && npm run build",
    "deploy": "npm run build:frontend && npm start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1"
  }
}
