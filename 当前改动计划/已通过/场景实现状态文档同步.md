# 场景实现状态文档同步

## 状态

已完成。

## 修改目标

- 清理根文档中“第二章、第三章、终章仍是占位”的旧说法。
- 将当前注册场景同步为 `landing`、`menu`、`prologue`、`chapter1`、`chapter1-workshop`、`chapter2`、`chapter2-workshop`、`chapter3`、`chapter3-workshop`、`finale`。
- 说明 `chapter-placeholder.js` 只是遗留占位文件，当前入口未注册使用。

## 涉及文件

- `README.md`
- `AGENTS.md`
- `CLAUDE.md`
- `Web/implementation_plan.md`

## 主要改法

- 更新目录树中 `pages/` 的场景文件列表。
- 更新“当前注册场景/前端状态”的描述。
- 不修改运行代码，不删除遗留文件。

## 完成标准

- 根文档不再把已实现章节描述为占位。
- 搜索验证只保留“遗留占位文件”的说明，不再出现“二/三/终章占位”的当前状态口径。
