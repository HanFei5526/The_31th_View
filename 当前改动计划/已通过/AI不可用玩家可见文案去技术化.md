# AI 不可用玩家可见文案去技术化

## 状态

已完成。

## 修改目标

- 后端未启动、未配置 Key 或网络不可用时，玩家界面不显示 `Web/server/.env`、`DEEPSEEK_API_KEY` 等技术配置说明。
- 保留控制台和文档里的技术排查信息。
- 终章结案笔记继续使用固定兜底文本，不影响主线结束。

## 涉及文件

- `Web/src/core/ai-service.js`
- `总分工文档/AI_提示词_后端数据设计.md`

## 主要改法

- 将 `AIService._callAPI()` 的默认不可用文案改为叙事化反馈。
- 将网络中断、上游失败、空返回的默认文案统一为玩家可理解的笔记本反馈。
- 同步 AI 设计文档中的降级策略说明。

## 完成标准

- 前端玩家可见默认文案不包含服务端路径或环境变量名。
- 服务层模拟后端不可用时，笔记本返回叙事化降级文本。
- 构建检查通过。
