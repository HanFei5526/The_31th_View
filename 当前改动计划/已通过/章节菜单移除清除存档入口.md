# 章节菜单移除清除存档入口

## 修改目标

当前项目暂未正式提供存档管理功能，因此章节菜单不再显示“清除存档”，也不保留对应确认弹窗逻辑。

## 涉及文件

- `Web/src/pages/menu.js`

## 主要改法

- 删除底部全局操作区中的 `btn-clear` 按钮。
- 删除 `btn-clear` 的事件绑定和清除存档确认逻辑。
- 不调整“重新开始”“继续游戏”按钮与章节选择逻辑。

## 完成标准

- 章节菜单不再出现“清除存档”。
- 源码中不再存在 `btn-clear`。
- `npm run build` 通过。
